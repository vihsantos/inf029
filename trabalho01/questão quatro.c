#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (){
	
	char Texto[250], Busca[100];
	int tamanho1, tamanho2;
	
		strcpy(Texto, "Eu so quero terminar logo esse trabalho");
		strcpy(Busca, "so");
		
		tamanho1=strlen(Texto);
		tamanho2=strlen(Busca);
	
}
